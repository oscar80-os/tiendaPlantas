async function load(){const r=document.getElementById('root'); try{const s=await db.collection('blog').where('activo','==',true).get(); let h=''; s.forEach(doc=>{const x=doc.data(); h+=`<div class="card"><h3>${x.titulo||'Artículo'}</h3><p>${x.resumen||''}</p>${x.linkExterno?`<p><a class="btn" href="${x.linkExterno}" target="_blank">Leer más</a></p>`:''}</div>`}); r.innerHTML=h||'<div class="card">No hay artículos.</div>';}catch(e){r.innerHTML='<div class="card">Error.</div>'}} window.addEventListener('DOMContentLoaded',load);
