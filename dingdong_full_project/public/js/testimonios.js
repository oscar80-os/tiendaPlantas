async function load(){const r=document.getElementById('root'); try{const s=await db.collection('testimonios').where('activo','==',true).get(); let h=''; s.forEach(doc=>{const x=doc.data(); h+=`<div class="card"><h3>${x.nombre||'Alumno'}</h3><p>${x.mensaje||''}</p><p>${'★'.repeat(Math.max(1,Math.min(5,Number(x.calificacion||5))))}</p></div>`}); r.innerHTML=h||'<div class="card">No hay testimonios.</div>';}catch(e){r.innerHTML='<div class="card">Error.</div>'}} window.addEventListener('DOMContentLoaded',load);
