function bind(){document.querySelectorAll('.q').forEach(b=>b.onclick=()=>b.parentElement.classList.toggle('open'));} async function load(){const r=document.getElementById('root'); try{const s=await db.collection('faq').where('activa','==',true).get(); let h=''; s.forEach(doc=>{const x=doc.data(); h+=`<div class="card ${''}"><div class="q" style="font-weight:bold;cursor:pointer">${x.pregunta||'Pregunta'}</div><div class="ans" style="margin-top:8px;display:none;color:#546e7a">${x.respuesta||''}</div></div>`}); r.innerHTML=h||'<div class="card">No hay FAQ.</div>'; document.querySelectorAll('.card .q').forEach(q=>q.onclick=()=>{const a=q.nextElementSibling; a.style.display=a.style.display==='block'?'none':'block';});}catch(e){r.innerHTML='<div class="card">Error.</div>'}} window.addEventListener('DOMContentLoaded',load);
