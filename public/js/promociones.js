function money(v){return new Intl.NumberFormat("es-CO",{style:"currency",currency:"COP",maximumFractionDigits:0}).format(Number(v||0));} async function load(){const r=document.getElementById('root'); try{const s=await db.collection('promociones').where('activa','==',true).get(); let h=''; s.forEach(doc=>{const x=doc.data(); h+=`<div class="card"><h3>${x.titulo||'Promo'}</h3><p>${x.descripcion||''}</p><p><strong>${money(x.precioPromo)}</strong></p></div>`}); r.innerHTML=h||'<div class="card">No hay promociones.</div>';}catch(e){r.innerHTML='<div class="card">Error.</div>'}} window.addEventListener('DOMContentLoaded',load);
